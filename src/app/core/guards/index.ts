/**
 * Barrel export pour tous les guards
 * Permet d'importer facilement les guards depuis un seul endroit
 * Exemple: import { authGuard, roleGuard } from '@core/guards';
 */
export * from './auth.guard';
export * from './role.guard';
